#
# Ansible managed
#
[tox]
minversion = 3.5
envlist = py{35,36,37,38}-ansible-{27,28,29,devel}
skipsdist = true

[testenv]
deps =
    devel: git+https://github.com/ansible/ansible.git@devel
    docker
    molecule
commands =
    molecule test
